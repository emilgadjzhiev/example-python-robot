inputs:
  robot_id:
    type: string

tasks:
  Run Python:
    shell: python tasks.py --robot_id ${{inputs.robot_id}}
    
condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
