inputs:
  robot_id:
    type: string

tasks:
  main:
    steps:
      - name: Run Python script
        robot: ./tasks.py
        arguments:
          robot_id: "${{inputs.robot_id}}"

condaConfigFile: conda.yaml
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
