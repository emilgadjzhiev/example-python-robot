tasks:
  - name: Run Python
    python: tasks.py
    inputs:
      input_data: "${json.loads(inputs['process_input'])}"


condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
